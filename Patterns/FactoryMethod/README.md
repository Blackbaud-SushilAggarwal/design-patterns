# Factory Method Pattern

This folder demonstrates the Factory Method pattern where a `RepositoryCreator` declares a factory method and concrete creators return concrete repository implementations.

Run:

```bash
dotnet run --project Patterns/FactoryMethod
```
